//Change the selected item so the user knows where there are
function selected(selectId, clearId){
	document.getElementById(selectId).className+=" sak-button-selected";
	document.getElementById(clearId).className="sak-button";
}

